{
  "name": "grafana-invite-gate",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test && wrangler deploy --dry-run --outdir /tmp/grafana-invite-worker-dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
